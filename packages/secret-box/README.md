# secret-box (MIGRATED)

The Secret Box / Faraday (L3 Power Trust) implementation has moved to its own
canonical repository:

```
<SECRET_BOX_REPO>
```

That repo is the single source of truth for Secret Box code, specs, recipes,
and runbook. Do not add Secret Box code here. The specs under
`ASC-Context-Engine/specs/Secret-Box-*.md` are frozen historical design notes;
the live canon lives in `<SECRET_BOX_REPO>/specs/`.
