# Runtime Boundary

This directory is a placeholder for documentation only.

Production runtime state does not belong in git.

Current deployed runtime is outside this repository:

```text
<CTX_BASE>
```

Do not copy task records, stdout logs, route ledgers, scopes, handoff files,
FRP configs, token files, or state files into this repository.
